# Addressing Inconsistent Labeling With Cross Image Matching for Scribble-Based Medical Image Segmentation
2025 TIP
近年来，弱监督学习在医学图像分割领域得到广泛应用，草图标注因能够大幅降低标注成本而备受青睐。但草图标注本身存在标注不完整、主观性强、缺乏统一规范等固有缺陷，会造成标注信息不一致，给网络训练带来巨大难题，最终严重影响分割模型性能。

针对该问题，本文提出**构建参考集**以引导像素级特征匹配：该参考集从多张带有不同草图标注的图像中提取**类别专属特征向量**与**像素级特征**搭建而成。参考集汇集了各类像素形态与目标类别特征，是像素级特征匹配方案的核心基础。借助该匹配策略，模型可对无标注像素进行有效特征比对，为标注不一致、标注残缺的训练场景提供可靠监督信号。

本文所提方法融合**平滑与回归**技术，实现跨图像像素特征对齐。依托丰富多样的像素样本，该匹配机制能够提升网络从参考集中学习统一特征模式的能力，缓解标注不一致、标注缺失带来的负面影响，显著提升分割效果。

本文在三组公开数据集上开展大量对比实验，结果表明，所提方法在分割精度与模型稳定性上均优于现有主流先进算法。相关代码已开源，项目地址：https://github.com/jingkunchen/scribble-medical-segmentation

# Boundary Perception Guidance: A Scribble-Supervised Semantic Segmentation Approach
2019 IJCAI
语义分割存在一个痛点：获取密集标注掩码的成本极高。为解决该问题，本文仅采用更易采集的涂鸦标注作为监督信息，实现分割模型的训练。

为充分挖掘涂鸦标注中有限的像素级标注信息，本文提出一种全新的**边界感知引导（Boundary Perception Guidance，BPG）** 方法，该方法包含两大基础模块，即 **预测优化模块** 与**边界回归模块**。具体而言，预测优化模块采用迭代上采样与语义特征增强策略，逐步优化分割结果；在边界回归模块中，本文使用类别无关边缘图作为监督信号，有效引导分割网络定位不同语义区域间的边界，进而生成适用于语义分割的细粒度特征图。

在PASCAL VOC 2012数据集上的实验结果表明，本文所提BPG方法在未结合全连接条件随机场（Conditional Random Field，CRF）时平均交并比（mIoU）达到73.2%，接入CRF后平均交并比为76.0%，刷新了现有公开文献中的最优性能指标。

# CycleMix: A Holistic Strategy for Medical Image Segmentation from Scribble Supervision
2022 CVPR
构建大规模完整标注训练数据集的成本高昂，医学图像分割任务尤为突出。涂鸦标注作为一种轻量化标注形式，在实际场景中更容易获取，但仅依靠少量涂鸦标注的弱监督条件训练分割模型仍存在诸多难点。

针对上述问题，本文提出一种基于涂鸦学习的全新医学图像分割框架，该框架融合**混合增强**与**循环一致性**模块，因此命名为CycleMix。

在监督信息增强层面，CycleMix采用混合插值策略，并专门设计随机遮挡机制，实现涂鸦标注的增量化与减量化处理。在监督信息正则化层面，该框架引入一致性损失强化训练目标，对分割结果不一致的情况施加惩罚，显著提升分割效果。

在ACDC与MSCMRseg两个公开数据集上的实验结果表明，本文所提方法性能表现优异，精度可与全监督方法持平，甚至更优。MSCMRseg数据集对应的代码与专家绘制涂鸦标注已开源，获取地址：https://github.com/BWGZK/CycleMix。

# DMSPS: Dynamically mixed soft pseudo-label supervision for scribble-supervised medical image segmentation 
2024 Medical Image Analysis
深度学习在医学图像分割任务中要实现优异性能，依赖大规模像素级密集标注。但标注过程繁琐、耗时，给医学专家带来极大工作量负担，处理**三维图像**时该问题尤为突出。为降低标注成本，同时保证分割效果处于较理想水平，基于稀疏标注的弱监督学习方法获得了越来越多的关注。

本文提出一种基于涂鸦标注的医学图像分割框架，命名为**动态混合软伪标签监督**（DMSPS）。具体来说，我们为骨干网络增设辅助解码器，构建双分支网络结构，以此提升共享编码器的特征提取能力。考虑到图像中绝大多数像素无标注，且硬伪标签容易出现过度置信问题，进而造成分割效果不佳，本文提出将两个解码器的预测结果动态融合生成软伪标签，作为模型的辅助监督信息。为进一步提升模型性能，我们采用两阶段训练策略：利用第一阶段模型低不确定性的预测结果扩充稀疏涂鸦标注，获得更多带标注像素用于第二阶段模型训练。

分别在心脏结构分割ACDC数据集、三维腹部器官分割WORD数据集、三维脑肿瘤分割BraTS2020数据集上开展实验，结果表明：（1）相较于基线方法，本文所提方法在上述三个数据集上的平均戴斯相似系数（DSC）分别由50.46%提升至89.51%、75.46%提升至87.56%、52.61%提升至76.53%；（2）动态混合软伪标签监督（DMSPS）的性能优于五种当前主流的涂鸦监督分割算法，且能够适配多种不同分割骨干网络。

相关代码已开源，地址：https://github.com/HiLab-git/DMSPS。

# EFFDNet: A Scribble-Supervised Medical Image Segmentation Method with Enhanced Foreground Feature Discrimination
2025 MICCAI
医学图像分割是医学图像分析中的核心任务，在辅助临床诊断流程中发挥关键作用。但传统全监督分割学习方法需要专业医师提供大量高质量标注数据，标注过程耗费大量人力与时间资源。为解决该问题，涂鸦监督分割方法采用简化标注方式以降低标注成本。然而，涂鸦标注信息过于简略，导致模型难以精准区分前景人体解剖结构与背景，也无法有效划分不同解剖类别。该缺陷会降低模型对前景形态的提取精度，同时削弱模型泛化性能。

针对上述问题，本文提出一种**增强前景特征区分网络（EFFDNet）**，该网络能够充分挖掘涂鸦标注中的语义信息，提升网络对前景的辨别能力。本文设计一种全新的**前景-背景分离损失函数（FBSL）**，强化模型区分前景、背景特征的能力，提升解剖前景区域形态识别的精准度。此外，本文提出**多样化上下文前景增强策略（FADC）**，进一步引导网络聚焦前景区域，丰富训练样本多样性，缓解模型过拟合问题并提升泛化能力。本文在两套公开数据集上开展完整实验验证，结果表明所提方法相较现有算法性能提升显著。相关代码开源地址：https://github.com/Aurora-003-web/EFFDNet

# Learning to Segment From Scribbles Using Multi-Scale Adversarial Attention Gates
2021 IEEE Transactions on Medical Imaging
像素级标注的大规模细粒度图像分割数据集难以获取，医学影像领域尤为突出，该领域的标注工作还需专业医师知识支撑。弱监督学习可依托线条涂鸦这类轻量化标注完成模型训练。

本文基于对抗博弈框架，利用线条涂鸦标注开展分割模型训练。在无配对分割掩码的条件下，我们训练多尺度生成对抗网络（GAN）生成多分辨率、真实自然的分割掩码，同时借助线条涂鸦约束掩码在图像内的正确位置。该模型性能的核心创新在于一种全新的注意力门控机制：以对抗信号作为该机制的条件约束，使其充当形状先验，大幅提升多尺度下目标物体的定位精度。在对抗条件约束下，分割网络能够生成具备语义信息的注意力图，抑制物体区域外的噪声激活值，同时缓解分割网络深层的梯度消失问题。

我们在多组医学数据集（ACDC、LVSC、CHAOS）与非医学数据集（PPSS）上完成模型验证，实验结果表明，本文模型性能与采用完整掩码标注训练的模型持平。此外，本文验证了该方法在多种场景下的拓展应用效果：半监督学习、融合多源涂鸦标注（众包标注场景）以及多任务学习（联合涂鸦与掩码监督）。我们已在网址https://vioss.github.io/multiscale-adversarial-attention-gates公开ACDC数据集的专业人工涂鸦标注文件与全部实验源代码。

# MedCL: Learning Consistent Anatomy Distribution for Scribble-supervised Medical Image Segmentation
2025 arXiv
构建大规模全标注数据集成本高昂、耗时费力且流程繁琐，医学图像领域尤为突出。现有文献中已提出多种利用涂鸦形式弱标注信息的算法，但这类方法需要大量涂鸦标注，且仅适用于常规器官分割；而服从长尾分布的各类病灶往往难以获取足量对应涂鸦标注。

鉴于医学标签具备解剖分布先验这一特性，本文提出一种基于涂鸦监督聚类的医学图像分割框架MedCL，用以学习医学标签内在的解剖分布规律。该方法分为两个阶段：一、通过图像内与图像间混合操作对特征进行混合增强；  
二、执行特征聚类，并从局部与全局两个维度约束解剖分布。  
搭配少量弱监督信息，本文所提MedCL框架可同时完成常规器官与难度较高的不规则病灶分割。本文基于SAM与UNet骨干网络实现MedCL，并在三类公开数据集上开展性能验证，分别为常规结构数据集MSCMRseg、多器官数据集BTCV以及不规则病灶数据集MyoPS。

实验结果表明，即便仅使用少量涂鸦监督数据，MedCL的分割效果仍显著优于传统分割算法。

相关代码已开源，地址：https://github.com/BWGZK/MedCL。

# ModelMix: A New Model-Mixup Strategy to Minimize Vicinal Risk Across Tasks for Few-Scribble Based Cardiac Segmentation
2024 MICCAI
像素级密集标注不仅耗费大量计算资源，还十分耗时，而涂鸦等弱标注相比完整标注具备更强的实用性。但基于涂鸦弱监督训练分割网络仍存在诸多难题。

受不同分割任务间存在关联性这一特性启发，本文提出一种**基于模型参数插值**的少量涂鸦监督分割新方法，命名为ModelMix。卷积核与偏置项的线性插值可实现对应特征向量线性插值，依托这一先验知识，ModelMix通过融合不同编码器的卷积参数构建虚拟模型。随后本文以无监督与涂鸦监督两种方式对模型组施加正则化约束，最小化跨任务邻域风险。

本文在ACDC、MSCMRseg、MyoPS三个公开数据集上完成验证，所提出的少量涂鸦引导ModelMix方法性能大幅超越现有最优涂鸦监督分割算法。相关代码已开源，地址：https://github.com/BWGZK/ModelMix

# Revisiting 3D Medical Scribble Supervision: Benchmarking Beyond Cardiac Segmentation
2025 MICCAI
涂鸦监督借助稀疏标注替代逐体素标签，成为降低三维医学分割标注成本的一种极具前景的方法。现有各类方法虽表现优异，但深入分析后可发现，绝大多数相关研究局限于心脏领域，主要采用ACDC与MSCMR数据集。这种研究方向过度单一化的问题引发了严重过拟合、性能提升结论存在误导性，同时模型难以在更多分割任务中实现泛化。

本文梳理了实用化涂鸦监督所需满足的一系列核心要求，并构建综合评测基准ScribbleBench，该基准涵盖七类不同的医学影像数据集，用于系统性验证现有方法能否满足上述要求。经评测发现，现有各类方法普遍存在跨任务泛化能力不足的问题；大量当下热门的创新模块脱离心脏场景后反而会降低模型性能，而一些被忽视的简易方法却具备更出色的泛化能力。最后，本文提出一套性能强劲却鲜少被关注的基线模型：结合局部损失函数的nnU-Net，该模型在各类多样化任务中的表现均持续优于各类专用改进方法。

本文揭示了现有研究存在的根本性缺陷，并建立一套全新的、基于基准测试的评估规范，旨在推动涂鸦监督技术发展出更实用、稳健、泛化性更强的医学图像分割算法。

# Scribble-Based 3D Multiple Abdominal Organ Segmentation via Triple-Branch Multi-Dilated Network with Pixel- and Class-Wise Consistency
2023 MICCAI
腹部计算机断层扫描（CT）图像多器官分割对腹部病灶诊断及后续治疗方案制定具有重要意义。尽管基于深度学习的相关方法已取得优异性能，但这类方法高度依赖大规模像素级标注，获取标注数据耗时耗力。弱监督分割对标注数据需求较低，因此受到广泛关注。然而现有弱监督分割方法与全监督学习方法之间仍存在显著性能差距，尚有较大研究探索空间。

本文提出一种含两项一致性约束的新型三维网络框架，用于基于涂鸦标注的腹部多器官CT图像分割。具体而言，本文采用**三分支多空洞卷积网络（TDNet）**，该网络包含一个编码器与三个采用不同扩张率的解码器，可提取来自不同感受野、具备互补性的特征，以此生成**高质量软伪标签**。为实现更稳定的无监督学习，本文**基于体素级不确定性修正软伪标签**，并利用修正后的标签对各解码器输出进行监督。为进一步对网络施加正则化约束，本文挖掘类别关联信息，通过约束多视图投影下不同解码器生成的类别亲和矩阵保持一致实现该目标。

在公开WORD数据集上开展的实验结果表明，本文所提方法性能优于五种现有基于涂鸦标注的分割算法。

# Scribble-Supervised Medical Image Segmentation via Dual-Branch Network and Dynamically Mixed Pseudo Labels Supervision
2022 MICCAI
医学图像分割在计算机辅助诊断、治疗规划与随访复查中发挥着不可替代的作用。构建并标注大规模数据集是训练高性能分割模型的关键，但生成高质量分割掩码成本高昂且十分耗时。近年来，基于稀疏标注（点标注、涂鸦标注、包围盒标注）开展网络训练的弱监督学习取得了可观效果，具备降低标注成本的潜力。然而，稀疏标注提供的监督信息有限，直接使用其训练网络仍存在较大挑战。

本文提出一种简洁高效的涂鸦监督图像分割方法，并将其应用于心脏磁共振图像分割任务。具体而言，本文搭建**双分支网络结构**，该网络包含一个编码器与两个存在细微差异的解码器用于图像分割；同时**动态融合**两个解码器的预测结果，生成**伪标签**作为辅助监督信息。结合涂鸦标注监督与伪标签辅助监督，该双分支网络可基于涂鸦标注完成高效端到端训练。在公开ACCDC数据集上开展的实验结果表明，本文所提方法性能优于现有各类涂鸦监督分割算法，同时优于多种半监督分割方法。

代码开源地址：https://github.com/HiLab-git/WSL4MIS

# Scribble-Supervised Semantic Segmentation Inference
2021 ICCV
本文提出一种**渐进式分割推理（PSI）**框架，用于解决涂鸦监督语义分割任务。该框架利用潜在上下文依赖关系，**融合上下文模式传播**与**语义标签扩散**两大核心线索，对由部分已知种子点生成的像素级分割结果进行增强与优化。在上下文模式传播模块中，基于图模型关联并利用不同粒度的上下文模式，实现模式信息的有效扩散，以此提升像素标签预测的推理置信度。在此基础上，依托预测像素的高置信度分值，通过动态学习自适应判定策略，将初始标注种子点逐步扩散至整张图像。最终将上述两大线索模块化，构建像素级标签推理过程中的闭环更新机制。

大量实验表明，本文所提渐进式分割推理框架能够融合空间与语义上下文线索，同时在两个公开涂鸦分割数据集上取得当前最优性能。

# Scribble2D5: Weakly-Supervised Volumetric Image Segmentation via Scribble Annotations
2022 MICCAI
基于涂鸦这类弱标注的图像分割方法受到广泛关注，原因在于相比像素/体素级耗时耗力的标注，涂鸦标注更易获取。但涂鸦标注缺少感兴趣区域（ROI）的结构信息，因此现有基于涂鸦的分割方法边界定位效果较差。此外，当前多数方法仅针对二维图像分割设计，无法充分利用体数据信息。

本文提出一种基于涂鸦的体图像分割网络Scribble2D5，用于解决**三维各向异性图像分割**问题并提升边界预测精度。为此，本文在**2.5D注意力UNet**基础上新增**标签传播模块**以拓展涂鸦携带的语义信息，同时融合静态边界预测与动态边界预测，实现感兴趣区域边界学习与形状正则化。

在三个公开数据集上开展大量实验，结果表明Scribble2D5性能显著优于现有基于涂鸦的分割方法，性能接近全监督分割算法。

相关代码已开源，地址：https://github.com/Qybc/Scribble2D5。

# Scribble2Label: Scribble-Supervised Cell Segmentation via Self-generating Pseudo-Labels with Consistency
2020 MICCAI
图像分割是显微细胞图像分析中的基础流程。随着深度学习相关技术近年来取得诸多进展，精度更高、通量更大的细胞分割技术得以实现。但现有的绝大多数基于深度学习的细胞分割算法都需要完整标注的细胞真值标签，制作这类标签耗时耗力。

本文提出一种全新弱监督细胞分割框架Scribble2Label，该框架仅依靠少量涂鸦标注，无需完整分割标签即可完成分割。其核心思路是融合伪标签生成与标签筛选技术，从弱监督信息中生成可靠标注。具体实现方式为利用模型预测结果的一致性，通过迭代平均预测值优化伪标签。

本文将Scribble2Label与多种当下顶尖的细胞分割算法在明场、荧光、电镜等多种模态细胞图像上开展对比实验，验证了该框架的分割性能。实验同时证明，该方法在不同精细程度的涂鸦标注下均能稳定输出良好效果，证实实际应用场景中仅需少量涂鸦标注即可满足使用需求。

# Scribbles for All: Benchmarking Scribble Supervised Segmentation Across Datasets
2024 NeurIPS
本文提出了Scribbles for All算法，这是一种基于涂鸦标注训练、用于语义分割的标注与训练数据生成算法。近年来，基于弱监督方式训练或微调语义分割模型已成为重要研究方向，相关模型性能也取得了大幅提升。在该研究场景下，涂鸦标注是一种极具应用前景的标注形式：它能够生成高质量分割结果，同时相比传统逐像素密集语义分割标注，所需标注工作量大幅降低。

将涂鸦作为弱监督数据源存在一个核心短板：目前缺少适用于涂鸦分割的**高难度数据集**，这阻碍了新型算法的研发与全面、客观的性能评估。为解决该问题，Scribbles for All为多款主流分割数据集配套生成了涂鸦标注，同时提供一套算法，可针对任意带有密集标注的数据集自动生成涂鸦标注，为弱监督分割领域挖掘新思路、推动模型性能突破奠定基础。

除数据集与生成算法外，本文还基于构建的数据集对当前顶尖分割模型开展对比实验。结果表明，采用本文合成标注训练得到的模型，性能与人工标注训练的模型不相上下。由此，本文提供的数据集能够支撑学界开展基于涂鸦标注语义分割方法的前沿研究。

相关数据集、涂鸦生成算法与基准模型代码已开源，项目地址：https://github.com/wbkit/Scribbles4All

# ScribbleSAM: Weakly supervised salient object detection and localization in remote sensing images using Transformer and Segment Anything Model
2025 Journal of Computational Design and Engineering
**光学遥感影像**（RSIs）在目标类别、朝向、尺寸以及复杂背景方面与自然场景图像（NSIs）存在显著差异。近年来，涂鸦标注被视作一种更高效的方案，用以减轻遥感影像显著目标检测（SOD）任务中像素级标注的工作量。但涂鸦标注所能提供的信息有限，会造成模型检测性能下降。

针对上述问题，本文结合分割一切模型（SAM）与Transformer模型，提出一种面向涂鸦标注遥感影像的新型弱监督显著目标分割模型ScribbleSAM。相较于现有弱监督模型，该模型能够大幅提升分割效果与精度。在预处理阶段，本文设计一种简洁且高效的方法：选取前景、背景涂鸦上的采样点以及前景涂鸦关键特征点，借助SAM自动生成伪掩码。随后联合涂鸦标注与生成的SAM伪掩码，对ScribbleSAM的双分支结构开展训练。其中，涂鸦标注用于训练涂鸦分支，以此捕捉显著目标的整体轮廓；SAM伪掩码用于训练SAM分支，优化模型对精细细节的预测结果，两个分支可实现优势互补。大量定量与定性实验结果表明，在两套遥感影像显著目标检测数据集上，本文所提模型性能优于现有各类弱监督模型，甚至可与全监督模型相媲美。此外，消融实验验证了该模型的有效性与泛化能力。

# ScribbleSup: Scribble-Supervised Convolutional Networks for Semantic Segmentation
2016 CVPR
大规模数据对于语义分割模型的训练至关重要，但逐像素掩码标注工作繁琐且效率低下。我们注意到，在交互式图像分割领域，线条标注在学术研究与商业软件中应用十分广泛，同时被公认为最易上手的人机交互方式之一。

本文提出采用线条标注对图像进行标注，并设计一套以线条标注为监督信号、用于训练语义分割卷积神经网络的算法。该算法依托图模型构建，可同步将线条标注信息传递至未标记像素并完成网络参数学习。依托线条标注完成训练后，我们在PASCAL VOC数据集上取得了具备竞争力的目标语义分割效果。对于水体、天空、草地这类无明确轮廓的背景类物体，线条标注同样具备优势；借助低成本的额外线条标注，本文方法在PASCAL-CONTEXT数据集上也取得了优异表现。我们基于PASCAL VOC数据集制作的线条标注数据集可访问链接获取：http://research.microsoft.com/en-us/um/people/jifdai/downloads/scribble_sup。

# ScribbleVC: Scribble-supervised Medical Image Segmentation with Vision-Class Embedding
2023 ACM MM
医学图像分割在临床决策、治疗方案规划以及病情监测中起到至关重要的作用。但受高质量标注数据稀缺、成像噪声、患者间解剖结构存在个体差异等多种因素影响，实现精准的医学图像分割难度较大。此外，现有基于少量标注的高效算法与全监督算法之间，性能仍存在明显差距。

为解决上述难题，本文提出一种全新的涂鸦监督医学图像分割框架**ScribbleVC**，该框架借助多模态信息增强机制融合视觉特征与类别嵌入特征。同时，ScribbleVC统一融合卷积神经网络特征与Transformer特征，实现更优质的视觉特征提取。该方法将涂鸦标注方案、分割网络与类别嵌入模块相结合，生成高精度分割掩膜。

本文在三组标准数据集上对ScribbleVC开展测试，并与当前主流先进算法进行对比。实验结果表明，该方法在分割精度、模型鲁棒性与运算效率方面均优于现有算法。相关数据集与代码已开源发布于GitHub平台。

# ScribFormer: Transformer Makes CNN Work Better for Scribble-Based Medical Image Segmentation
2024 IEEE Transactions on Medical Imaging
目前主流的涂鸦监督分割算法普遍采用编码器-解码器架构的卷积神经网络框架。该框架虽具备诸多优势，但卷积层感受野具有局部性，仅能捕捉小范围特征依赖关系，难以依托涂鸦标注提供的有限信息学习全局形状特征。

针对该问题，本文提出一种适用于涂鸦监督医学图像分割的**卷积神经网络-Transformer混合模型**，命名为ScribFormer。本文所提ScribFormer模型采用**三分支结构**，由**卷积神经网络分支**、**Transformer分支** 与**注意力引导类激活图（ACAM）** 分支融合构成。具体而言，卷积神经网络分支与Transformer分支协同工作，将卷积神经网络提取的局部特征和Transformer获取的全局表征进行融合，能够有效突破现有涂鸦监督分割算法存在的局限性。此外，注意力引导类激活图分支可实现浅层卷积特征与深层卷积特征的统一融合，进一步提升模型分割性能。

本文在两个公开数据集与一个私有数据集上开展大量对比实验，结果表明，所提ScribFormer模型性能优于当前主流的涂鸦监督分割算法，分割效果甚至超越全监督分割算法。

相关代码已开源，开源地址：https://github.com/HUANGLIZI/ScribFormer。

# ShapePU: A New PU Learning Framework Regularized by Global Consistency for Scribble Supervised Cardiac Segmentation
2022 MICCAI
心脏分割是心血管疾病诊断的关键步骤。但逐像素密集标注成本高昂且耗时。涂鸦标注作为一种稀疏标注形式，相比全量标注更易获取。然而，基于涂鸦弱监督训练分割网络存在极大挑战。

针对该问题，本文基**于Positive-Unlabeled（PU）学习框架**与**全局一致性正则化**，提出一种全新的涂鸦引导心脏分割方法，命名为ShapePU。为借助PU学习利用无标注像素，本文首先采用**期望最大化（EM）算法**估算无标注像素中各类别的占比。基于估算得到的占比，进一步引入边缘概率最大化策略判定无标注像素所属类别。为挖掘形状先验知识，本文对训练图像执行裁剪操作，并对不一致的分割结果施加惩罚。

在ACDC、MSCMRseg两个公开数据集上开展实验验证，本文提出的涂鸦监督ShapePU方法平均戴斯系数（Dice）相较全监督方法分别提升1.4%、9.8%，同时大幅优于当前主流弱监督与PU学习算法。

相关代码开源地址：https://github.com/BWGZK/ShapePU。

# Soft Self-labeling and Potts Relaxations for Weakly-supervised Segmentation
2025 CVPR
本文研究弱监督图像分割任务，该任务中仅有部分像素具备真实标注（涂鸦标注）；本文重点研究一种自标注方法，该方法针对无标注像素优化标准无监督条件随机场/波茨损失的松弛形式。弱监督语义分割方法虽可通过梯度下降直接优化这类损失，但现有研究表明，引入隐伪标签与高性能条件随机场子问题求解器（如图割算法）的高阶优化方式，能够提升网络训练效果。然而，以往采用的硬伪标签无法表征类别不确定性与标注误差，由此催生了**软自标注**方案。

本文推导了具备理论依据的辅助损失函数，并系统对比评估多种经典与新型条件随机场松弛模型（含凸模型与非凸模型）、邻域系统，以及用于关联网络预测结果与软伪标签的损失项；同时提出一种通用连续子问题求解器。仅采用基础网络架构时，软自标注方法即可持续提升基于涂鸦标注的模型训练效果，性能远超结构复杂度更高的专用弱监督语义分割系统，甚至优于全像素精准标注的监督方案。本文提出的通用思路同样可适用于其他各类弱监督学习任务与算法框架。

# Sparsely Annotated Semantic Segmentation with Adaptive Gaussian Mixtures
2023 CVPR
稀疏标注语义分割（SASS）旨在利用仅有稀疏标签（即点标注或线条涂鸦标注）的图像训练分割模型。现有方法大多侧重于引入底层像素相似关系或生成伪标签以增强监督信号，却很大程度上忽略了已标注像素与未标注像素之间固有的关联。

本文研究发现，特征空间中距离相近的像素更大概率属于同一类别。受该发现启发，我们提出一种全新的稀疏标注语义分割框架，框架内置**自适应高斯混合模型（AGMM）**。该自适应高斯混合模型能够依据已标注像素与未标注像素的特征分布，为未标注像素提供可靠的监督信息。具体流程如下：首先以各类别的已标注像素作为聚类中心，结合与其特征相近的未标注像素构建高斯混合分布，以此建模每一类别的特征分布；随后结合已标注像素的可信信息与自适应生成的高斯混合模型预测结果，对未标注像素的训练过程实施监督，实现实时、动态且鲁棒的自监督学习。除此之外，通过构建分类别高斯混合分布，自适应高斯混合模型以端到端对比学习的方式，引导模型学习具备区分度的类别决策边界。

在PASCAL VOC 2012与Cityscapes数据集上开展的实验结果表明，本文所提自适应高斯混合模型刷新了稀疏标注语义分割任务的当前最优性能。相关代码开源地址：https://github.com/Luffy03/AGMM-SA

# SparseMamba-PCL: Scribble-Supervised Medical Image Segmentation via SAM-Guided Progressive Collaborative Learning
2025 arXiv
涂鸦标注能够大幅降低包含复杂解剖结构的大型医疗数据集进行密集标注所需的成本与人力。但现有基于涂鸦监督的学习方法难以将稀疏标注标签有效扩散为密集分割掩码，同时无法精准分割目标边界。

针对上述问题，本文提出一种**渐进式协同学习框架**，该框架结合创新算法与MedSAM基础模型，提升训练过程中的信息质量。  
（1）设计全新算法扩充真值涂鸦分割标签，通过扩散涂鸦来预估目标边界；  
（2）融合Med-SAM与本文提出的稀疏曼巴网络的特征嵌入，优化由Med-SAM引导的训练流程，以此增强特征表征能力；经过扩充的涂鸦标签也可借助该优化表征完成Med-SAM解码器的微调；  
（3）推理阶段引入稀疏曼巴网络，该网络摒弃传统逐序列分块处理方式，采用跳跃采样机制，具备极强的局部与全局依赖捕捉能力。

本文在ACDC、CHAOS、MSCMRSeg数据集上开展实验，结果验证了该框架的有效性，性能优于九种当前最优算法。相关代码已开源，仓库地址为SparseMamba-PCL.git。

# Tree Energy Loss: Towards Sparsely Annotated Semantic Segmentation
2022 CVPR
稀疏标注语义分割（SASS）旨在利用粗粒度监督信息（即点标注、涂鸦标注、块状标注）训练分割网络，每张图像中仅有少量像素被标注。

本文针对稀疏标注语义分割任务提出一种全新的**树能量损失函数**，为未标注像素提供语义引导。该树能量损失将图像表征为最小生成树，以此建模底层与高层的成对像素亲和关系。通过将上述亲和关系依次作用于网络预测结果，由粗到细地生成未标注像素的软伪标签，实现动态在线自训练。将该树能量损失与传统分割损失结合后，可简便嵌入现有算法框架，且具备出色性能。相较于过往稀疏标注语义分割方法，本文方法无需多阶段训练策略、交替优化流程、额外标注数据以及耗时的后处理操作，在各类稀疏标注语义分割实验场景下性能均优于现有方法。

代码开源地址：https://github.com/megvii-research/TreeEnergyLoss

# Weakly-Supervised Concealed Object Segmentation with SAM-based Pseudo Labeling and Multi-scale Feature Grouping
2023 NeurIPS
弱监督隐蔽目标分割（WSCOS）旨在利用标注稀疏的数据训练模型，精准分割与周边环境高度融合的目标。该任务至今仍存在较大难点：（1）隐蔽目标与背景本身特征高度相似，难以区分；（2）用于训练的稀疏标注数据仅能为模型学习提供弱监督信号。本文提出一种全新的弱监督隐蔽目标分割方法，用以解决上述两大难题。

针对目标与背景固有特征相似的问题，本文设计多尺度特征分组模块：先以不同粒度对特征分组，再融合各组输出结果。该模块将相近特征归为一类，保证分割区域的完整性，无论是单目标图像还是多目标图像，都能输出完整的分割结果。

针对弱监督带来的训练难题，本文采用近期提出的视觉基础模型——分割一切模型（SAM），将稀疏标注作为提示词生成分割掩码，并以此掩码训练模型。为降低低质量分割掩码带来的负面影响，本文进一步提出多项优化策略：多增强结果集成、基于信息熵的像素级加权、基于信息熵的图像筛选。上述策略能够为分割模型的训练提供更可靠的监督信息。

本文在多项弱监督隐蔽目标分割任务上验证所提方法的有效性，实验结果表明该方法在各类任务中均达到当前最优性能。相关代码已开源，地址：https://github.com/ChunmingHe/WS-SAM

# Weakly-Supervised Domain Adaptive Semantic Segmentation with Prototypical Contrastive Learning
2023 CVPR
针对语义分割任务的无监督域自适应性能优化，学界已开展大量研究工作，但该方法与有监督学习相比仍存在显著性能差距。

本文提出一种通用框架，可利用目标域内图像级、点级、粗粒度标注等**多种弱标签**来缩小这一性能差距。具体而言，我们借助上述弱标签学习更具**类别代表性**的**优质原型特征**，并基于优化后的原型完成类别特征的对比对齐。我们设计两类差异化特征对齐操作：一是在单个域内完成像素特征与原型的对齐；二是以非对称方式将源域像素特征对齐至目标域原型。这种非对称对齐方式可在训练过程中保留目标域特征，当目标域仅存在弱标签时，该特性至关重要。在多组标准测试集上开展的实验结果表明，本文所提框架相较现有方法实现大幅性能提升，有效缩小了与有监督学习之间的效果差距。

相关代码将开源至https://github.com/anurag-198/WDASS

# ZScribbleSeg: A comprehensive segmentation framework with modeling of efficient annotation and maximization of scribble supervision
2026 Medical Image Analysis
构建完整标注的医学图像分割数据集需要耗费大量人力，且对专业知识要求极高。为缓解该难题，现有研究已探索采用涂鸦标注实现弱监督分割。现有方法主要在标注区域计算损失，并将标注信息扩散至相邻区域以生成伪标签。但受监督信息不足、形状信息缺失影响，这类方法分割结果常存在精度低、分割轮廓不符合真实器官形态的问题。

与之不同，本文首先分析优质涂鸦标注的核心原理，通过最大化监督信息与模拟随机分布**设计出高效涂鸦标注形式**；同时引入**正则项对空间关联与形状约束进行建模**，并采用期望最大化（EM）算法估算各类标签的混合比例。该比例对筛选各类未标注像素、修正错误预测至关重要，精准的比例估算也为融入空间先验信息奠定基础。最终，本文将高效涂鸦监督机制与空间先验整合为一套名为ZScribbleSeg的算法框架，并应用于多种医学影像场景。

仅依靠涂鸦标注，ZScribbleSeg在六项分割任务（**ACDC、MSCMRseg、BTCV、MyoPS、Decathlon-BrainTumor、Decathlon-Prostate**）上均取得具备竞争力的实验效果。相关代码将通过网址https://zmiclab.github.io/projects.html开源。